// script.js

document.addEventListener('DOMContentLoaded', () => {
  const productList = document.getElementById('productList');

  if (productList) {
      // Here you can fetch and display the product list
      productList.innerHTML = '<p>Product List is loaded successfully.</p>';
  } else {
      console.error('The productList element does not exist.');
  }
});
